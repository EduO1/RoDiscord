services:
  - type: web
    name: rodiscord
    env: node
    buildCommand: npm install
    startCommand: node backend/rodiscord_backend_v2.js
    envVars:
      - key: NODE_ENV
        value: production
